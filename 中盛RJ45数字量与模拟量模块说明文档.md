# 中盛 RJ45 数字量与模拟量模块说明文档

本文根据当前文件夹中的两份原始手册整理：

- `数字量输入输出系列使用手册(RJ45版).pdf`
- `模拟量输入系列使用手册(网口版).pdf`

适用对象：中盛科技 RJ45 网口版数字量输入输出模块、模拟量输入模块。本文偏向现场接线、上位机/PLC 调试和 Modbus 寄存器快速查阅。

## 1. 快速结论

两类模块的共同默认参数如下：

| 项目 | 默认值/说明 |
| --- | --- |
| 工作模式 | TCP Server / 服务端 |
| IP 地址 | `192.168.0.7` |
| 端口 | `8234` |
| 通讯接口 | 隔离型 10M RJ45 网口 |
| 协议 | Modbus TCP、Modbus RTU |
| 参数复位 | 复位按钮 / 软件复位 |
| 工作温度 | `-40℃ ~ +85℃` |
| 工作湿度 | `0% ~ 95%RH`，无凝结 |

调试时先把电脑网卡设置到同一网段，例如：

- 电脑 IP：`192.168.0.100`
- 子网掩码：`255.255.255.0`
- 模块 IP：`192.168.0.7`
- 连接端口：`8234`

如果现场同时有多个出厂默认模块，先单独接入并逐个修改 IP，避免 `192.168.0.7` 地址冲突。

## 2. 文件夹内容

| 文件 | 用途 |
| --- | --- |
| `数字量输入输出系列使用手册(RJ45版).pdf` | 原始数字量模块手册 |
| `模拟量输入系列使用手册(网口版).pdf` | 原始模拟量模块手册 |
| `数字量输入输出系列使用手册(RJ45版).txt` | 从 PDF 提取的数字量手册文本，便于搜索 |
| `模拟量输入系列使用手册(网口版).txt` | 从 PDF 提取的模拟量手册文本，便于搜索 |
| `中盛RJ45数字量与模拟量模块说明文档.md` | 本整理文档 |

## 3. 典型气检系统接入建议

在中央净软气检测试这类场景中，可按如下方式理解两类模块：

| 现场对象 | 推荐接入模块 | 说明 |
| --- | --- | --- |
| `4~20mA` 气体浓度传感器 | 模拟量输入模块 | 读取 AI 通道采样值，再换算为电流和浓度 |
| `0~5V / 0~10V` 电压型传感器 | 模拟量输入模块 | 直接按电压量程解析 |
| 报警灯、蜂鸣器、排风、阀门、继电器线圈 | 数字量输出模块 | 使用线圈或保持寄存器控制开关 |
| 干接点报警、门磁、按钮、限位开关 | 数字量输入模块 | 读取离散输入或输入寄存器 |

注意：模块的电气规格、传感器量程、负载电流必须与现场设备一致。继电器输出和晶体管输出接线方式不同，不能混用。

## 4. 数字量输入输出模块

### 4.1 产品能力

- 支持 `1~48` 路数字量输入检测。
- 支持 `1~48` 路继电器或晶体管输出控制。
- 输入支持 PNP 高电平触发、NPN 低电平触发。
- 输入支持干接点和湿接点。
- 继电器输出额定负载可选 `5A / 10A / 16A / 30A / 50A`。
- 晶体管输出为 PNP 输出，最大负载 `23A / 100V / 140W`。
- 支持功能码：`01`、`02`、`03`、`04`、`05`、`06`、`0F`、`10`。

### 4.2 数字量接线

| 标识 | 功能 |
| --- | --- |
| `+` | 供电电源正极。8 路及以下通常为 `6~36V DC` 宽压；8 路以上通常有 `12V DC` 或 `24V DC` 规格 |
| `-` | 供电电源负极 |
| `COM/CN` | 输出公共端 |
| `NO` | 继电器常开端 |
| `NC` | 继电器常闭端 |
| `COM` | 输入信号公共端 |
| `X` | 信号输入端 |

输入公共端 `COM` 的触发逻辑：

| 接法 | 触发类型 | 说明 |
| --- | --- | --- |
| `COM` 接电源正极 | NPN / 低电平触发 | 输入端拉低时触发 |
| `COM` 接电源负极 | PNP / 高电平触发 | 输入端拉高时触发 |

继电器输出按 `COM/CN`、`NO`、`NC` 接入负载回路。晶体管输出为 PNP 型，用于控制电源正极侧。

### 4.3 数字量输入读取

数字量输入可用两种寄存器读取。

离散输入状态寄存器，功能码 `02`：

| 协议地址 | PLC 地址 | 含义 |
| --- | --- | --- |
| `0000H` | `10001` | 第 1 路输入状态 |
| `0001H` | `10002` | 第 2 路输入状态 |
| `...` | `...` | 依次类推 |
| `002FH` | `10048` | 第 48 路输入状态 |

输入寄存器，功能码 `04`：

| 协议地址 | PLC 地址 | 含义 |
| --- | --- | --- |
| `0000H` | `30001` | 第 1 路输入状态 |
| `0001H` | `30002` | 第 2 路输入状态 |
| `...` | `...` | 依次类推 |
| `002FH` | `30048` | 第 48 路输入状态 |
| `0032H` | `30051` | 按位表示第 `1~16` 路输入状态，最低位为第 1 路 |
| `0033H` | `30052` | 按位表示第 `17~32` 路输入状态，最低位为第 17 路 |
| `0034H` | `30053` | 按位表示第 `33~48` 路输入状态，最低位为第 33 路 |

状态值含义：

- `0`：未触发
- `1`：已触发

示例，读取第 1 路输入状态，手册给出的 RTU 报文：

```text
发送：01 04 00 00 00 01 31 CA
未触发返回：01 04 02 00 00 B9 30
已触发返回：01 04 02 00 01 78 F0
```

### 4.4 数字量输出控制

线圈寄存器，功能码 `01 / 05 / 0F`：

| 协议地址 | PLC 地址 | 含义 |
| --- | --- | --- |
| `0000H` | `00001` | 第 1 路输出控制 |
| `0001H` | `00002` | 第 2 路输出控制 |
| `...` | `...` | 依次类推 |
| `002FH` | `00048` | 第 48 路输出控制 |

线圈值含义：

- `0`：关闭输出
- `1`：开启输出

保持寄存器，功能码 `03 / 06 / 10`：

| 协议地址 | PLC 地址 | 复位值 | 含义 |
| --- | --- | --- | --- |
| `0000H` | `40001` | `0` | 第 1 路输出控制 |
| `0001H` | `40002` | `0` | 第 2 路输出控制 |
| `...` | `...` | `0` | 依次类推 |
| `002FH` | `40048` | `0` | 第 48 路输出控制 |
| `0034H` | `40053` | `0` | 批量控制，`0` 全关，`1` 全开 |
| `0035H` | `40054` | `0` | 按位控制第 `1~16` 路输出 |
| `0036H` | `40055` | `0` | 按位控制第 `17~32` 路输出 |
| `0037H` | `40056` | `0` | 按位控制第 `33~48` 路输出 |

普通模式下，单路保持寄存器控制值：

- `0`：关闭
- `1`：开启
- `>1`：延时切换，延时时间为 `(N - 1) * 0.01s`

常用 RTU 示例：

```text
第 1 路继电器吸合，06 写保持寄存器：
01 06 00 00 00 01 48 0A

第 1 路继电器吸合，05 写线圈：
01 05 00 00 FF 00 8C 3A

第 1~2 路继电器吸合，10 写多个保持寄存器：
01 10 00 00 00 02 04 00 01 00 01 63 AF

第 1~4 路继电器吸合，0F 写多个线圈：
01 0F 00 00 00 04 01 0F 7E 92
```

### 4.5 数字量模块参数寄存器

| 协议地址 | PLC 地址 | 复位值 | 功能 |
| --- | --- | --- | --- |
| `0030H` | `40049` | `0` | 通讯检测时间。`0` 不检测；大于 1 时，通讯断开 `N * 0.1s` 后所有输出关闭 |
| `0031H` | `40050` | `0` | 输入状态主动上传。`0` 不上传；`1` 输入变化时上传；大于 1 时按 `(N - 1) * 0.01s` 周期上传 |
| `0032H` | `40051` | `1` | RS485 总线地址/站号，范围 `1~255` |
| `0033H` | `40052` | `1` | 波特率设置 |
| `0096H` | `40151` | `0` | 第 1 路控制模式 |
| `0097H` | `40152` | `0` | 第 2 路控制模式 |
| `...` | `...` | `0` | 依次类推 |
| `00C6H` | `40198` | `0` | 第 48 路控制模式 |

波特率枚举：

| 值 | 波特率 |
| --- | --- |
| `0` | `4800` |
| `1` | `9600`，出厂默认 |
| `2` | `14400` |
| `3` | `19200` |
| `4` | `38400` |
| `5` | `56000` |
| `6` | `57600` |
| `7` | `115200` |

通道控制模式：

| 值 | 模式 | 说明 |
| --- | --- | --- |
| `0` | 普通模式 | 指令直接控制输出 |
| `1` | 联动模式 | 输入通道和指令均可控制输出；输入触发时同号输出开启 |
| `2` | 点动/翻转模式 | 写 `1` 时输出状态翻转 |
| `3` | 开关循环模式 | 按设定时间循环开关 |
| `4` | 自动复位模式 | 开启固定时长后自动关闭 |

参数类寄存器掉电保存，修改后建议重新上电确认生效。

## 5. 模拟量输入模块

### 5.1 产品能力

- 支持电压、电流采集，多量程、多通道可选。
- 采集路数 `1~18` 路。
- AD 采集位数 `12` 位。
- 数据更新速率 `30Hz/通道`。
- 输入方式为差分输入。
- 支持功能码：`03`、`04`、`06`、`10`。

常见量程和精度：

| 项目 | 参数 |
| --- | --- |
| 供电电压 | `6~36V DC` |
| 功耗 | `<200mW` |
| 电压量程 | `0~500mV / 0~5V / 0~10V / 0~30V / 0~100V / 0~500V DC` 等 |
| 电流量程 | `0~20mA / 4~20mA / 0~2A / 0~5A / 0~10A` 等 |
| 电压精度 | `±1‰` |
| `0~20mA / 4~20mA` 电流精度 | `±1‰` |
| `0~2A / 0~5A / 0~10A` 电流精度 | `±1%` |

### 5.2 模拟量接线

| 标识 | 功能 |
| --- | --- |
| `+` | 供电电源正极，`6~36V DC` |
| `-` | 供电电源负极 |
| `RJ45` | 网口通讯 |
| `AI+` | 被测电压/电流正极 |
| `AI-` | 被测电压/电流负极 |

电压/电流切换说明：

- 5V 电压采集与 `0~20mA / 4~20mA` 电流采集可切换。
- 每路内置跳线帽，可独立设置为电压或电流采集。
- 跳线帽不短接：电压采集。
- 跳线帽短接：电流采集。
- 出厂默认：电压采集。

接入气体传感器时，常见 `4~20mA` 两线/三线传感器需按传感器说明书供电，并把采样回路正确接到对应 `AI+ / AI-`。不要把超过模块量程的电压或电流直接接入。

### 5.3 模拟量输入寄存器

输入寄存器使用功能码 `04`，每个通道占 1 个 16 位无符号整数。

| 协议地址 | PLC 地址 | 含义 |
| --- | --- | --- |
| `0000H` | `30001` | 第 1 路采样值 |
| `0001H` | `30002` | 第 2 路采样值 |
| `0002H` | `30003` | 第 3 路采样值 |
| `...` | `...` | 依次类推 |
| `0011H` | `30018` | 第 18 路采样值 |

单位：

- 电压采集：解析后单位为 `V`。
- 电流采集：解析后按量程处理，`0~20mA / 4~20mA` 模块需先按采样电阻换算。

### 5.4 模拟量数据解析

模块支持两类解析方式。

可变小数点表示法，出厂默认：

- 最高位数字，也就是万位，表示小数点位数。
- 例：寄存器值 `31000`，万位 `3` 表示 3 位小数，数值部分 `1000`，实际值为 `1000 * 0.001 = 1V`。

固定小数点表示法：

- 由保持寄存器 `003AH / 40059` 设置。
- 固定 2 位、3 位或 4 位小数。
- 例：固定 3 位小数时，寄存器值 `1000` 解析为 `1000 * 0.001 = 1V`。

`0~20mA / 4~20mA` 电流模块换算：

```text
先把寄存器值解析为采样电阻两端电压 Vsample，单位 V
实际电流 I = Vsample / 249Ω
例：Vsample = 1V
I = 1 / 249 = 0.004016A = 4.016mA
```

如果要换算气体浓度，需要再根据传感器量程计算。例如传感器为 `4~20mA = 0~100%LEL`：

```text
浓度百分比 = (实际电流 mA - 4) / 16 * 100
```

实际项目中应按传感器铭牌或说明书的量程、零点、满量程单位换算。

### 5.5 模拟量模块参数寄存器

保持寄存器使用功能码 `03 / 06 / 10`。

| 协议地址 | PLC 地址 | 复位值 | 功能 |
| --- | --- | --- | --- |
| `0031H` | `40050` | `0` | 主动上传时间间隔，单位 `0.01s`；`0` 不主动上传 |
| `0032H` | `40051` | `1` | 设备 ID，范围 `1~255` |
| `0033H` | `40052` | `1` | 波特率设置 |
| `003AH` | `40059` | `0` | 数据解析方式设置 |
| `003DH` | `40062` | `0` | 奇偶校验设置 |

波特率枚举与数字量模块相同：

| 值 | 波特率 |
| --- | --- |
| `0` | `4800` |
| `1` | `9600`，出厂默认 |
| `2` | `14400` |
| `3` | `19200` |
| `4` | `38400` |
| `5` | `56000` |
| `6` | `57600` |
| `7` | `115200` |

数据解析方式枚举：

| 值 | 解析方式 |
| --- | --- |
| `0` | 可变小数点，出厂默认 |
| `1` | 固定 2 位小数 |
| `2` | 固定 3 位小数 |
| `3` | 固定 4 位小数 |

奇偶校验枚举：

| 值 | 校验 |
| --- | --- |
| `0` | 无校验，出厂默认 |
| `1` | 奇校验 |
| `2` | 偶校验 |

参数类寄存器掉电保存，修改后建议重新上电确认生效。

### 5.6 模拟量常用 RTU 示例

```text
修改站号为 2：
01 06 00 32 00 02 A9 C4

修改波特率为 38400：
01 06 00 33 00 04 78 06

修改数据解析方式为固定 3 位小数：
01 06 00 3A 00 02 28 06

读取第 1 路数据：
01 04 00 00 00 01 31 CA

响应示例：
01 04 02 03 E8 B9 8E

数据 0x03E8 = 十进制 1000
固定 3 位小数时：1000 * 0.001 = 1
```

## 6. 网口参数设置流程

网口参数需要使用厂商的“中盛综合测试系统”。

通用步骤：

1. 模块上电，网线连接模块到电脑或交换机。
2. 把电脑网卡设到模块同一网段，例如 `192.168.0.100/255.255.255.0`。
3. 使用默认地址 `192.168.0.7:8234` 连接模块。
4. 在测试系统左侧菜单选择“通讯参数”。
5. 点击“网口参数设置”。
6. 根据需要选择 `TCP/IP-Server`、`TCP/IP-Client` 或 MQTT Client 相关模式。
7. 点击保存后等待 `5~8s`，看到参数设置成功提示。
8. 给模块断电约 `5s` 后重新上电，使参数生效。

TCP Server 模式常用参数：

| 参数 | 说明 |
| --- | --- |
| 模块监听 IP | 模块自身 IP |
| 模块监听端口 | 模块服务端口 |
| 子网掩码 | 一般为 `255.255.255.0` |
| 模块网关 | 与现场局域网网段匹配 |

TCP Client 模式常用参数：

| 参数 | 说明 |
| --- | --- |
| 服务器地址 | 上位机或服务器 IP/域名 |
| 服务器端口号 | 上位机或服务器监听端口 |
| 模块 IP 地址 | 模块自身 IP |
| 模块端口号 | 模块本地端口 |
| 子网掩码 | 一般为 `255.255.255.0` |
| 默认网关 | 与现场局域网网段匹配 |
| DNS 服务器 | 可用 `114.114.114.114` 或 `8.8.8.8` |

## 7. Modbus TCP 与 RTU 使用提醒

原手册中的报文示例多为 Modbus RTU 格式，末尾包含 CRC 校验码。

实际使用时按软件或 PLC 的协议模式选择：

- 使用标准 Modbus TCP：通常只需填写 IP、端口、功能码、寄存器地址和数量，CRC 由协议栈处理或不需要手工填写。
- 使用 RTU over TCP 或透明传输：通常发送包含从站地址和 CRC 的 RTU 帧，类似手册示例。
- 使用 PLC 地址时注意偏移：协议地址 `0000H` 往往对应 PLC 地址 `30001`、`40001` 或 `00001`，具体取决于 PLC/组态软件是否从 0 开始寻址。

建议第一次联调时优先读取只读寄存器：

1. 模拟量模块读取 `0000H / 30001`，确认第 1 路采样值有变化。
2. 数字量模块读取 `0000H / 30001` 或离散输入 `0000H / 10001`，确认输入状态变化。
3. 再测试数字量输出控制，先空载或接小功率指示灯验证逻辑。

## 8. 现场调试检查表

上电前：

- 确认模块供电电压符合型号规格。
- 确认输入信号没有超过模块量程。
- 确认继电器或晶体管输出负载电流不超过规格。
- 确认模拟量电压/电流跳线帽位置正确。
- 确认电脑、PLC、模块 IP 在同一网段。

联机时：

- 先 `ping 192.168.0.7` 或目标模块 IP。
- 确认端口 `8234` 可连接。
- 先读寄存器，再写寄存器。
- 改 IP、端口、站号、波特率、解析方式等参数后，断电重启模块。

异常排查：

| 现象 | 优先检查 |
| --- | --- |
| 连接不上模块 | IP 网段、地址冲突、端口、网线、交换机、电脑防火墙 |
| 读数一直为 0 | 传感器供电、AI+/AI- 极性、量程、跳线帽、寄存器地址 |
| `4~20mA` 数值不对 | 是否按 `V / 249Ω` 换算，传感器是否实际输出电流 |
| 输入状态反了 | NPN/PNP 接法，输入 COM 接正还是接负 |
| 输出不动作 | 输出类型、负载供电、COM/NO/NC 接线、保持寄存器/线圈地址 |
| 改参数后无效 | 是否等待保存成功，是否断电 5 秒后重启 |

## 9. 推荐上位机数据点命名

| 点名示例 | 模块 | 地址 | 说明 |
| --- | --- | --- | --- |
| `AI_GAS_01_RAW` | 模拟量 | `30001` | 第 1 路原始采样值 |
| `AI_GAS_01_MA` | 模拟量 | 计算点 | 第 1 路换算电流 mA |
| `AI_GAS_01_VALUE` | 模拟量 | 计算点 | 第 1 路换算后的气体浓度 |
| `DI_ALARM_01` | 数字量 | `10001` 或 `30001` | 第 1 路输入报警 |
| `DO_FAN_01` | 数字量 | `00001` 或 `40001` | 第 1 路风机/继电器输出 |
| `DO_BUZZER_01` | 数字量 | `00002` 或 `40002` | 第 2 路蜂鸣器输出 |

命名可按实际设备改为区域、柜号、传感器编号、报警等级等结构，便于后续排查。

