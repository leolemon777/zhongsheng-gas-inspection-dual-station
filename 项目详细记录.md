# 中央净软气检工装项目详细记录

最后更新：2026-05-25

## 1. 项目当前状态

- 项目名称：中央净软气检工装 / 中盛气检上位机。
- 当前阶段：现场调试版已可运行，已完成 Win10 x64 自包含打包。
- 目标设备：Windows 10 64 位工控机，屏幕分辨率 1920x1080。
- 运行方式：解压发布包后直接运行 exe，不需要在工控机安装 .NET 运行环境。
- 当前硬件结论：IO 模块继电器输出只负责触点通断，不主动输出电压；阀不动作的问题已确认是阀本体问题。
- 后续计划：接入扫码、产品追溯、查询/导出、必要时对接 MES 或数据库。

## 2. 关键路径

| 项目 | 路径 |
| --- | --- |
| 工作根目录 | `E:\Desktop\中央净软气检测试` |
| 软件工程目录 | `E:\Desktop\中央净软气检测试\中盛气检上位机` |
| 解决方案 | `E:\Desktop\中央净软气检测试\中盛气检上位机\ZhongshengGasInspectionHmi.sln` |
| 配置文件 | `E:\Desktop\中央净软气检测试\中盛气检上位机\Data\appsettings.json` |
| 本机调试 exe | `E:\Desktop\中央净软气检测试\中盛气检上位机\src\ZhongshengGasInspectionHmi.UI\bin\Debug\net8.0-windows\ZhongshengGasInspectionHmi.UI.exe` |
| 发布目录 | `E:\Desktop\中央净软气检测试\中盛气检上位机\dist\中央净软气检工装-win10-x64-20260523` |
| 发布 zip | `E:\Desktop\中央净软气检测试\中盛气检上位机\dist\中央净软气检工装-win10-x64-20260523.zip` |
| 实现记录 | `E:\Desktop\中央净软气检测试\implementation-notes.md` |
| 模块说明整理 | `E:\Desktop\中央净软气检测试\中盛RJ45数字量与模拟量模块说明文档.md` |
| 压力变送器说明书 | `E:\Desktop\3229498717OHR-M系列小巧型压力变送器说明书(2025.09.03).pdf` |

## 3. 技术栈与工程结构

- 技术栈：.NET 8、WPF、CommunityToolkit.Mvvm、Microsoft.Data.Sqlite。
- UI 主文件：`src\ZhongshengGasInspectionHmi.UI\MainWindow.xaml`。
- 自动流程：`Services\GasInspectionRunner.cs`。
- 真实硬件控制：`Services\ZhongshengInspectionHardware.cs`。
- Modbus TCP/RTU over TCP 客户端：`Services\ZhongshengModbusTcpClient.cs`。
- 生产主页状态：`ViewModels\RunPageViewModel.cs`。
- IO 手动监控：`ViewModels\IoMonitorPageViewModel.cs`。
- 配置与记录路径：`Services\AppStoragePaths.cs`。
- 检测记录数据库：`Services\InspectionRecordStore.cs`。

## 4. 当前硬件配置

当前 `Data\appsettings.json` 中的主要配置：

| 项目 | 当前值 | 说明 |
| --- | --- | --- |
| IO 模块 IP | `192.168.0.7` | RJ45 数字量模块 |
| IO 模块端口 | `8234` | Modbus 端口 |
| 模拟量模块 IP | `192.168.0.22` | 本机现场已改为 0.22 |
| 模拟量模块端口 | `8234` | Modbus 端口 |
| Modbus 单元号 | `1` | Unit Id |
| 协议模式 | `UseRtuOverTcp=true` | 现场模块按 RTU over TCP 通讯 |
| 开阀输出 | `InletOpenCoil=1` | DO2 / 协议地址 0001H |
| 关阀输出 | `InletCloseCoil=0` | DO1 / 协议地址 0000H |
| 阀动作通电时间 | `5000 ms` | 开阀/关阀最长通电 5 秒 |
| 压力输入 | `PressureRegister=0` | AI1 / 30001 |
| 模拟量小数 | `AnalogFixedDecimalPlaces=3` | 按当前模块显示格式 |

## 5. 当前工艺参数

| 参数 | 当前值 |
| --- | --- |
| 充气时间 | `5 s` |
| 稳压时间 | `5 s` |
| 保压时间 | `5 s` |
| 允许漏率 | `0.0022`，按 `(P1-P2)/P1` 小数值判断 |
| 4mA 对应压力 | `0.000 MPa` |
| 20mA 对应压力 | `1.000 MPa` |
| 最小升压保护 | `0.000 MPa` |
| 最大压力保护 | 当前流程中不拦截 |
| 自动排气 | `false`，现场手动排气 |

说明：当前版本为了现场干跑测试，压力、电流、升压异常、超压等保护不打断流程。正式量产前如果需要保护，应重新确认并恢复对应策略。

## 5.1 压力变送器核对

已核对 `OHR-M2系列小巧型压力变送器使用说明书`：

- 型号系列：OHR-M2 小巧型压力变送器。
- 输出信号：`4～20mA（二线制）`，也可选电压输出；当前现场按 4-20mA 二线制使用。
- 工作电压：`DC18～36V`。
- 测量范围：说明书标准量程表包含 `F2 0～1.0MPa`，当前软件配置为 `4mA=0MPa`、`20mA=1MPa`，与 0-1MPa 量程匹配。
- 精度等级：说明书列出 `0.2`、`0.5` 级，真实测量准确度受精度等级、温漂、长期稳定性和模拟量模块采样分辨率影响。
- 软件处理：内部仍以 MPa 计算压力和漏率，但采样保留到 6 位 MPa；生产页、P1/P2、记录页按 kPa 显示，保留 3 位小数。
- 注意：显示到 0.001kPa 不等于设备真实精度到 1Pa。若现场工艺真的要求 1Pa 级准确度，需要重新核对变送器精度等级、量程是否过大、模拟量模块分辨率和标定方案。

## 6. 继电器与阀接线理解

IO 模块的 Y 输出是继电器触点，本质是无源开关：

```text
DO 关闭：COM 和 NO 不通
DO 开启：COM 和 NO 导通
```

模块本身不会从 NO 端主动输出 24V。若要让阀线得到 24V，需要把外部 24V 正极串入继电器触点。

当前三线二控阀按“公共负极、开/关线给 24V 动作”的接法理解：

```text
24V+ ──┬── Y1 COM
       │
       └── Y2 COM

Y1 NO ───── 阀关线
Y2 NO ───── 阀开线

24V- ────── 阀公共线
```

注意事项：

- `NC` 不接。
- `Y1 NO` 和 `Y2 NO` 不能并在一起。
- 当前现场确认后按 DO2 管开阀线，DO1 管关阀线配置。
- 模块自身供电的 `+/-` 仍然要单独接 24V 电源。
- 若后续更换阀，必须先确认阀线定义：是“开/关线加 24V 动作”，还是“阀自身供电后只需要干接点控制”。两种接法完全不同。

## 7. 自动检测流程

当前自动流程为：

1. 点击启动。
2. 进入充气步骤，同时启动开阀动作，开阀线最多通电 5 秒。
3. 充气时间到后进入稳压步骤，同时启动关阀动作，关阀线最多通电 5 秒。
4. 稳压结束后采集 P1，并在界面显示 P1 采集步骤约 1 秒。
5. 进入保压步骤，实时显示当前步骤剩余时间、步骤已用/总时长、总运行时间。
6. 保压结束后采集 P2，并在界面显示 P2 采集步骤约 1 秒。
7. 计算漏率：`(P1 - P2) / P1`；若 P1 为 0，当前测试版漏率记为 0，避免除零。
8. 根据允许漏率输出 OK / NG。
9. 保存检测记录。
10. 结束后执行关阀动作，排气仍由现场手动处理。

停止逻辑：

- 按停止后立即取消当前流程。
- 后台执行关阀动作。
- UI 释放启动按钮，下次启动从第一步重新开始。

阀动作逻辑：

- 阀不是“一上电就全开/全关”，而是在通电过程中持续运动。
- 开阀/关阀动作都按配置时间通电，当前 5 秒。
- 如果开阀动作未结束时又要关阀，软件会取消开阀通电，立即切换到关阀通电；手动页同理。

## 8. UI 当前约定

- 生产主页：
  - 显示实时压力、mA、P1、P2、漏率、判定。
  - 压力统一按 kPa 显示，保留 3 位小数。
  - 检测数据区域同时显示实际漏率和参数设定的允许漏率，便于现场对比。
  - 检测结果为 NG 时，主页面弹出遮罩警示，显示大号 NG、产品条码、实际漏率、允许漏率，必须点击确认后才关闭。
  - 显示当前步骤剩余时间、步骤已用/总时长、总运行时间。
  - 每个流程卡片显示自己的时间。
  - 等待步骤为浅色，进行中为金色/橙色，完成为深绿色。
- 参数设置：
  - 可设置充气、稳压、保压、允许漏率、压力量程等。
  - 当前页面提示为测试模式：异常数据不拦截流程。
- IO 监控：
  - 自动/手动区分明显。
  - 手动模式下 DO1/DO2 可操作，DO3/DO4 锁定。
  - 手动开/关阀过程中可直接切换方向。
- 硬件连接：
  - 配置 IO/模拟量模块 IP、端口、协议、点位。
  - 可连接、断开、读取压力。
- 通信日志：
  - 可查看 Modbus TX/RX 帧。
- 检测记录：
  - 当前保存到 SQLite 数据库。

## 9. 已解决的问题记录

- 交换机连接：模块可通过交换机连接，需确认 PC 和模块同网段。
- 模拟量地址：默认说明是 `192.168.0.7`，本机实际模拟量模块为 `192.168.0.22`。
- Modbus TCP 解析：修正 MBAP length 处理，避免响应帧读取错误。
- 阀动作时间：从短脉冲改为最长 5 秒持续通电。
- 阀动作互锁：开/关方向可以中途切换，软件会先断当前方向再开另一方向。
- 自动流程：第一步直接充气计时，不再启动前额外等待关阀。
- P2 记录：P1/P2 都有明确采集步骤。
- 时间显示：每个流程步、当前步骤、总运行时间均显示。
- 干跑测试：删除/绕过压力异常保护，方便无气源时完整跑流程。
- 停止后启动：停止不再长时间卡住，可重新启动。
- 手自动显示：IO 页自动/手动状态更明显。
- 进行中颜色：进行中不再用深绿色，深绿色只代表完成。
- 发布部署：生成 Win10 x64 自包含 zip 包。

## 10. 当前发布包说明

发布包：

```text
E:\Desktop\中央净软气检测试\中盛气检上位机\dist\中央净软气检工装-win10-x64-20260523.zip
```

发布目录内容：

```text
中央净软气检工装-win10-x64-20260523\
  ZhongshengGasInspectionHmi.UI.exe
  ZhongshengGasInspectionHmi.UI.pdb
  Data\
    appsettings.json
```

部署方式：

1. 把 zip 拷到 Win10 x64 工控机。
2. 解压整个文件夹。
3. 运行 `ZhongshengGasInspectionHmi.UI.exe`。
4. 首次运行会自动在 `Data` 下创建 `inspection-records.db`。

## 11. 常用命令

调试编译：

```powershell
dotnet build "E:\Desktop\中央净软气检测试\中盛气检上位机\ZhongshengGasInspectionHmi.sln"
```

打开调试 exe：

```powershell
Start-Process -FilePath "E:\Desktop\中央净软气检测试\中盛气检上位机\src\ZhongshengGasInspectionHmi.UI\bin\Debug\net8.0-windows\ZhongshengGasInspectionHmi.UI.exe"
```

Win10 x64 自包含发布：

```powershell
dotnet publish "E:\Desktop\中央净软气检测试\中盛气检上位机\src\ZhongshengGasInspectionHmi.UI\ZhongshengGasInspectionHmi.UI.csproj" -c Release -r win-x64 --self-contained true -o "E:\Desktop\中央净软气检测试\中盛气检上位机\dist\中央净软气检工装-win10-x64-20260523" /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true /p:EnableCompressionInSingleFile=true
```

## 12. 后续扫码与追溯建议

后续需求需要确认：

- 扫码枪类型：USB 键盘、串口 COM、网口 TCP。
- 条码规则：长度、前缀、是否允许空码、是否允许重复码。
- 启动规则：扫码后自动启动，还是扫码后手动按启动。
- 重复检测：同一条码是否允许复测，NG 后是否允许再次测试。
- 记录字段：产品条码、工位号、操作员、设备编号、治具编号、P1、P2、漏率、判定、开始/结束时间、参数版本。
- 数据去向：本地 SQLite 查询、CSV/Excel 导出、MES HTTP 接口、TCP 接口或数据库。
- 追溯查询：按条码、时间、OK/NG、工位筛选。
- 异常处理：扫码失败、重复码、未扫码启动、MES 上传失败、离线缓存重传。

建议实现顺序：

1. USB 键盘扫码枪先接入产品条码框。
2. 启动前校验条码。
3. 检测记录表增加追溯字段。
4. 检测记录页增加条码/时间/结果筛选。
5. 增加 CSV/Excel 导出。
6. 最后按现场接口要求对接 MES。

## 13. 2026-05-25 参数小数输入修正

现场发现“允许漏率”输入 `0.0022` 时，小数点不容易输入。

原因：

- 参数页数字框原来使用 `UpdateSourceTrigger=PropertyChanged`。
- WPF 会在每输入一个字符时立刻把文本转换为数字。
- 输入到 `0.` 这种中间状态时，文本还不是最终数字，容易触发校验红框并回退。

处理：

- 参数页所有主要数字输入框改为失去焦点后再提交数值。
- 包括充气时间、稳压时间、保压时间、允许漏率、压力量程、安全拔管压力、排气观察时间、最小充气升压、最大允许压力。
- 这样可以正常输入 `0.0022`、`0.6` 等小数，点击“保存配置”时输入框会先失去焦点并提交当前值。

## 14. 2026-05-25 漏率显示与判断单位统一

现场发现主界面两个“允许漏率”卡片带 `%` 显示，而测试结果有些位置不带 `%`，单位不统一。

原因：

- 漏率公式本身是 `(P1-P2)/P1`，天然就是小数值。
- 旧界面按百分比显示，并且配置值还会 `/100` 后参与判断。
- 如果现场输入 `0.0022`，旧逻辑会当成 `0.0022%`，实际阈值变成 `0.000022`，判断会偏严。

处理：

- 参数页去掉“允许漏率 %”中的 `%`。
- 主界面允许漏率、实际漏率、NG 弹窗、检测记录页统一显示纯小数，不带 `%`。
- 判断逻辑按配置的小数值直接比较；输入 `0.0022`，就按 `0.0022` 判断。

## 15. 2026-05-25 主页面待机压力实时刷新

现场发现生产主页在流程外不会跟随压力表实时变化，只有流程运行期间才持续刷新压力。

处理：

- 生产主页增加待机压力刷新：硬件已连接且流程未运行时，自动读取模拟量压力。
- 启动流程时不再把实时压力和电流显示硬清为 0，避免刚点击启动时读数跳变。
- 硬件服务增加压力读取互斥，避免生产主页、硬件连接页和流程同时读模拟量模块。

## 16. 2026-05-25 压力显示平滑与 P1/P2 平均采样

现场发现类似 `575.050 kPa` 的压力值，小数点后三位会因模拟量末位噪声跳动。

处理：

- 主页面实时压力显示使用最近 5 次读数移动平均。
- 主页面待机刷新频率调整为 0.2 秒一次，5 次平均约覆盖 1 秒窗口，兼顾稳定和实时跟随。
- P1 和 P2 不再只读一次，改为各自连续读取 5 次、间隔 100ms，取平均后记录。
- 漏率计算使用平均后的 P1/P2，降低单次瞬时波动导致 OK/NG 误判的风险。
- 流程提示中会标注 P1/P2 为“5次平均”，方便现场理解和追溯。
