# 中央净软气检工装调试摘要

最后更新：2026-05-23

## 1. 软件位置

- 项目目录：`E:\Users\Administrator\Desktop\中央净软气检测试\中盛气检上位机`
- 当前 EXE：`E:\Users\Administrator\Desktop\中央净软气检测试\中盛气检上位机\src\ZhongshengGasInspectionHmi.UI\bin\Debug\net8.0-windows\ZhongshengGasInspectionHmi.UI.exe`
- 配置与记录目录：`E:\Users\Administrator\Desktop\中央净软气检测试\中盛气检上位机\Data`
- 配置文件：`Data\appsettings.json`
- 检测记录库：`Data\inspection-records.db`

## 2. 硬件规划

- IO 模块：RJ45 4IO 数字量模块。
- 模拟量模块：4-20mA 2 通道模拟量模块。
- 压力表：4-20mA 输出，接模拟量模块 AI1。
- 进气阀：三线自保持阀，软件用双 DO 脉冲控制。
- 排气：暂时现场手动排气，软件不写排气阀。

## 3. 交换机与 IP 规划

两个模块出厂默认 IP 都可能是 `192.168.0.7`，不能同时直接插入交换机，否则 IP 冲突。

推荐地址：

| 设备 | IP | 端口 |
| --- | --- | --- |
| 工控机网口 | `192.168.0.100` | - |
| IO 模块 | `192.168.0.10` | `8234` |
| 模拟量模块 | `192.168.0.11` | `8234` |

改 IP 步骤：

1. 先只接一个模块，不要两个默认 IP 模块同时上交换机。
2. 电脑网口设为 `192.168.0.100`，掩码 `255.255.255.0`。
3. 用厂家综合测试工具连接默认地址 `192.168.0.7:8234`。
4. 在网口参数设置里改模块 IP。
5. 保存后等待 5-8 秒，再断电重启模块。
6. 第二个模块单独重复以上步骤。
7. 两个模块 IP 改完后，再一起接入交换机。

## 4. 进气自保持阀接线

软件点位：

| 点位 | 功能 | 软件动作 |
| --- | --- | --- |
| DO1 / 地址 `0000H` | 进气阀打开线 | ON 500ms 后 OFF |
| DO2 / 地址 `0001H` | 进气阀关闭线 | ON 500ms 后 OFF |
| DO3 / DO4 | 备用 | 软件锁定 |

接线：

```text
24V+ -> DO1 COM
DO1 NO -> 进气阀打开线
DO1 NC -> 不接

24V+ -> DO2 COM
DO2 NO -> 进气阀关闭线
DO2 NC -> 不接

进气阀公共负极 -> 24V-
```

注意：

- 不使用 NC。
- DO1 和 DO2 不能同时输出。
- 该阀是自保持阀，脉冲到位后会保持状态，不需要长期通电。

## 5. 模拟量压力接线

模拟量模块端子按说明书：

| 标识 | 功能 |
| --- | --- |
| `+` | 模块电源正极，6-36V DC |
| `-` | 模块电源负极 |
| `AI+` | 被测电压/电流正极 |
| `AI-` | 被测电压/电流负极 |

调试要求：

- 压力表 4-20mA 信号接 AI1。
- 模拟量模块对应通道跳线帽要设为电流采集模式。
- 出厂默认可能是电压采集，接线前必须确认跳线。
- 软件按说明书使用 249 欧采样电阻换算：`mA = V / 249 * 1000`。

## 6. 自动检测逻辑

停止后再次启动，永远从头开始，不从中间续跑。

流程：

1. 启动前先发送进气阀关闭脉冲。
2. 充气开始发送进气阀打开脉冲。
3. 充气结束发送进气阀关闭脉冲。
4. 稳压结束采集 `P1`。
5. 保压结束采集 `P2`。
6. 计算漏率：`(P1 - P2) / P1`。
7. 漏率小于等于设定值为 OK，大于设定值为 NG。
8. 结束后保持进气阀关闭，现场手动排气。

停止/异常：

- 立即终止本轮流程。
- 发送进气阀关闭脉冲。
- 下次启动重新开始完整流程。

## 7. 现场首次调试顺序

1. 先不要接气，先通电测试模块通讯。
2. 在硬件连接页填：
   - 数字量模块 IP：`192.168.0.10`
   - 模拟量模块 IP：`192.168.0.11`
   - 端口：`8234`
3. 进入 IO 监控页，切手动。
4. 点 DO1，确认进气阀切到打开。
5. 点 DO2，确认进气阀切到关闭。
6. 进入模块连接页读取压力，确认 AI1 有合理 mA 与 MPa。
7. 再接低压气源，调压阀先开低压。
8. 手动确认排气阀能泄压后，再跑自动流程。

## 8. 当前软件页面约定

- 生产主页：启动、停止、手动排气提示、P1/P2/漏率/结果。
- 参数设置：充气、稳压、保压、允许漏率、压力量程。
- IO 监控：只显示 RJ45 4IO 的 DI1-DI4、DO1-DO4。
- 模块连接：配置模块 IP、端口、协议、点位、读取压力。
- 通信日志：查看 Modbus TX/RX 帧。
- 检测记录：查看历史检测记录。

